# ## Server Properties
# server.port= 5000
# server.compression.enabled=true

# ## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
# spring.datasource.url= jdbc:mysql://localhost:3306/vx_leave?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
# spring.datasource.username= root
# spring.datasource.password= Pramuditha1*


# ## Hibernate Properties
# # The SQL dialect makes Hibernate generate better SQL for the chosen database
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# spring.jpa.hibernate.ddl-auto = update

# ## Hibernate Logging
# logging.level.org.hibernate.SQL= DEBUG

# # Initialize the datasource with available DDL and DML scripts
# spring.datasource.initialization-mode=always

# ## Jackson Properties
# spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
# spring.jackson.time-zone= UTC

# ## App Properties
# app.jwtSecret= JWTSuperSecretKey
# app.jwtExpirationInMs = 604800000

# # Mail
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=lahrukasun8@gmail.com
# spring.mail.password=Pramuditha1*

# # Other properties
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.connectiontimeout=10000
# spring.mail.properties.mail.smtp.timeout=10000
# spring.mail.properties.mail.smtp.writetimeout=10000

# # TLS , port 587
# spring.mail.properties.mail.smtp.starttls.enable=true

# login_url = http://localhost:3000/

# logging.level.com.testapi.api=info
# logging.appender.file=org.apache.log4j.RollingFileAppender
# logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{10} : %msg%n
# logging.file=./logs/logfile.log
