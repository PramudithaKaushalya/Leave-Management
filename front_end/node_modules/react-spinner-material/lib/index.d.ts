/// <reference types="react" />
import './inject';
interface Props {
    size: number;
    spinnerColor: string;
    spinnerWidth: number;
    visible: boolean;
    [key: string]: any;
}
declare const Spinner: ({ visible, spinnerColor, spinnerWidth, size, ...rest }: Props) => JSX.Element | null;
export default Spinner;
